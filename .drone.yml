---
kind: pipeline
name: amd64

platform:
  os: linux
  arch: amd64

steps:
- name: build
  image: plugins/docker
  settings:
    username:
      from_secret: DOCKER_USERNAME
    password:
      from_secret: DOCKER_PASSWORD
    repo: lansible/zigbee2mqtt
    build_args:
    - VERSION=${DRONE_TAG:-master}
    tags: ${DRONE_TAG:-master}-amd64

---
kind: pipeline
name: arm64

platform:
  os: linux
  arch: arm64

steps:
- name: build
  image: plugins/docker
  settings:
    username:
      from_secret: DOCKER_USERNAME
    password:
      from_secret: DOCKER_PASSWORD
    repo: lansible/zigbee2mqtt
    build_args:
    - VERSION=${DRONE_TAG:-master}
    tags: ${DRONE_TAG:-master}-amd64

---
kind: pipeline
name: manifest

platform:
  os: linux
  arch: amd64

steps:
- name: manifest-versioned
  image: plugins/manifest
  settings:
    username:
      from_secret: DOCKER_USERNAME
    password:
      from_secret: DOCKER_PASSWORD
    target: lansible/zigbee2mqtt:${DRONE_TAG:-master}
    template: lansible/zigbee2mqtt:${DRONE_TAG:-master}-ARCH
    platforms:
    - linux/amd64
    - linux/arm64
  when:
    branch:
    - master

- name: manifest-latest
  image: plugins/manifest
  settings:
    username:
      from_secret: DOCKER_USERNAME
    password:
      from_secret: DOCKER_PASSWORD
    target: lansible/zigbee2mqtt:latest
    template: lansible/zigbee2mqtt:${DRONE_TAG:-master}-ARCH
    platforms:
    - linux/amd64
    - linux/arm64
  when:
    event:
    - tag

depends_on:
- amd64
- arm64
